#include<stdio.h>
#include<stdlib.h>
#include<time.h>

/**
 * main - Entry point
 * Reaturn: Always 0 (Success)
 */

int main(void)
{       
        int i, p, q;
        for(i = '0'; i < '9'; i++)
	{
	for(p = i+1; p <= '9'; p++)
	{
	if ((p != i) != q)
	{
	putchar(i);
	putchar(p);
	putchar(q);
	if (d == '7' && p == '8')
	continue;
	putchar(',');
	putchar(' ');
	}
	}
	}
	putchar('\n');
	return(0);
}
